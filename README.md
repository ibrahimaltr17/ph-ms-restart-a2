What is JSX, and why is it used?

Ans: JSX is a way to write HTML inside JavaScript in React. It is used for write HTML similer code in JavaScript.


What is the difference between State and Props?

Ans: State is data inside a component and Props are data passed from a parent component to a child component.


What is the useState hook, and how does it work?

Ans: useState is a React it Hook help to store and update data inside a component. When state changes, React updates the screen automatically.


How can you share state between components in React?

Ans: Move the state to their common parent component and pass it down using props.


How is event handling done in React?

Ans: In React, event handling is done by attaching functions to events like onClick, onChange or onSubmit. When the event happens, the function runs
